spring:
  jpa:
    show-sql: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    atomdance.app: DEBUG
    org.hibernate.SQL: WARN
    org.springframework.security: WARN