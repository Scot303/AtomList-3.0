/**
 * What a student status is worth, as a whole-number percentage - 10 means 10%.
 *
 * Mirrors `DiscountRules.STUDENT_PERCENT` on the backend, which is what is actually applied.
 */
export const STUDENT_DISCOUNT_PERCENT = 10;
