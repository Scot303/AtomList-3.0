/**
 * Caps on how much filtering one table can carry, overridable per table through {@link DataTableProps}.
 * They exist for readability rather than performance.
 */
export const DEFAULT_MAX_FILTER_TAGS = 5;

export const DEFAULT_MAX_ADVANCED_RULES = 7;
